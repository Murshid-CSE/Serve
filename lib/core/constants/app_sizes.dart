class AppSizes {
  AppSizes._();

  // ── SPACING ──────────────────────────────────────────────────────────
  static const double spacingXs = 4.0;
  static const double spacingSm = 8.0;
  static const double spacingMd = 16.0;
  static const double spacingLg = 24.0;
  static const double spacingXl = 32.0;
  static const double spacingXxl = 48.0;

  // ── PADDING ──────────────────────────────────────────────────────────
  static const double paddingXs = 4.0;
  static const double paddingSm = 8.0;
  static const double paddingMd = 16.0;
  static const double paddingLg = 24.0;
  static const double paddingXl = 32.0;
  static const double paddingXxl = 48.0;

  /// Horizontal screen padding used for page-level content.
  static const double screenPaddingH = 16.0;

  /// Vertical screen padding used for page-level content.
  static const double screenPaddingV = 24.0;

  // ── BORDER RADIUS ────────────────────────────────────────────────────
  static const double radiusSm = 8.0;
  static const double radiusMd = 12.0;
  static const double radiusLg = 16.0;
  static const double radiusXl = 24.0;
  static const double radiusCard = 16.0;
  static const double radiusButton = 12.0;
  static const double radiusChip = 20.0;
  static const double radiusBottomSheet = 24.0;
  static const double radiusDialog = 16.0;
  static const double radiusTextField = 12.0;
  static const double radiusCircle = 999.0;

  // ── ICON SIZES ───────────────────────────────────────────────────────
  static const double iconSm = 16.0;
  static const double iconMd = 24.0;
  static const double iconLg = 32.0;
  static const double iconXl = 48.0;
  static const double iconXxl = 64.0;

  // ── ELEVATION ────────────────────────────────────────────────────────
  static const double elevationSm = 1.0;
  static const double elevationMd = 2.0;
  static const double elevationLg = 4.0;
  static const double elevationXl = 8.0;

  // ── COMPONENT HEIGHTS ────────────────────────────────────────────────
  static const double appBarHeight = 56.0;
  static const double bottomNavHeight = 64.0;
  static const double buttonHeight = 52.0;
  static const double buttonHeightSm = 40.0;
  static const double textFieldHeight = 56.0;
  static const double chipHeight = 36.0;
  static const double cardMinHeight = 80.0;
  static const double cardMaxHeight = 200.0;
  static const double listTileHeight = 72.0;
  static const double minTouchTarget = 48.0;
  static const double fabSize = 56.0;
  static const double avatarSm = 32.0;
  static const double avatarMd = 48.0;
  static const double avatarLg = 64.0;
  static const double avatarXl = 96.0;
  static const double avatarProfile = 120.0;

  // ── WIDTHS ───────────────────────────────────────────────────────────
  static const double maxContentWidth = 600.0;
  static const double maxDialogWidth = 400.0;
  static const double dividerThickness = 0.5;
  static const double borderWidth = 1.0;
  static const double borderWidthThick = 2.0;

  // ── IMAGE SIZES ──────────────────────────────────────────────────────
  static const double imageThumbSize = 80.0;
  static const double imageMediumSize = 200.0;
  static const double imageLargeSize = 400.0;
  static const double emptyStateImageSize = 200.0;
  static const double onboardingImageSize = 280.0;

  // ── MAP SPECIFIC ─────────────────────────────────────────────────────
  static const double mapDefaultZoom = 14.0;
  static const double mapMinZoom = 5.0;
  static const double mapMaxZoom = 20.0;
  static const double mapMarkerSize = 40.0;
  static const double mapClusterSize = 48.0;

  /// Search radius for food donations in kilometers.
  static const double mapSearchRadiusFood = 5.0;

  /// Search radius for blood requests in kilometers.
  static const double mapSearchRadiusBlood = 25.0;

  /// Search radius for volunteer tasks in kilometers.
  static const double mapSearchRadiusVolunteer = 10.0;

  /// Search radius for emergency requests in kilometers.
  static const double mapSearchRadiusEmergency = 15.0;

  // ── ANIMATION DURATIONS (ms) ─────────────────────────────────────────
  static const int animDurationFast = 150;
  static const int animDurationNormal = 300;
  static const int animDurationSlow = 500;
  static const int animDurationPage = 400;

  // ── PAGINATION ───────────────────────────────────────────────────────
  static const int defaultPageSize = 20;
  static const int leaderboardPageSize = 50;
}
